import { ProductFullImageStackProps } from "../Types";

const ProductFullImage = ({ route }: ProductFullImageStackProps) => {
  return <div>ProductFullImage</div>;
};

export default ProductFullImage;
